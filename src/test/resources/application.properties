
# WARNING: KEEP IT, EVEN IF NO PROPERTY IS SET, BECAUSE WITHOUT THIS FILE H2 TESTS WON'T WORK!!!

#==== Spring Boot properties ====#
# Super hot, but not super crucial during testing :-)
spring.main.banner-mode=off

#==== Server properties ====#

#==== Database connection (MySQL) ======#
# Nothing here. Default values provided by Spring Boot and the test starter seem to be fine enough :)

#==== Logging ====#
# Next line should not be needed if tests are annotated with @DataJpaTest, which already enables spring.jpa.show-sql=true
#logging.level.org.hibernate.SQL=DEBUG
# logs Hibernate's query parameters
#logging.level.org.hibernate.type.descriptor.sql=TRACE

#==== Application properties ====#
sharpink.resources.webUrl=webUrl_TEST
sharpink.resources.fileSystemPath=fileSystemPath_TEST
